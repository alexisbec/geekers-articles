<p id="notice"><%= notice %></p>

<div class="cat-index-grid">
  <% @categories.each do | category | %>
    <%= link_to category.name, category_path(category.id), class: "index-grid-ele" %>
  <% end %>
</div>
